/*
using BeatKeeper.Utils;
using System;
using System.Diagnostics;
using Xunit;

namespace BeatKeeper.Tests
{
    public class PathTests
    {
        [Fact]
        public void GeneratesPathCorrectly()
        {
            var path = PathUtils.GetResourcePath("hello", "world");

            Assert.NotNull(path);
            Assert.True(path.EndsWith("hello\\world") || path.EndsWith("hello/world"));
        }
    }
}
*/